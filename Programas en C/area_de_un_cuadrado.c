#include <stdio.h>
#include <math.h>

int main(){
    int l, area;
    printf("Ingrese el lado del cuadrado: ");
    scanf("%d", &l);
    area = l * l;
    printf("El area del cuadrado es: %i\n",area);
    #tipos de servidores el servidor web, es el que proporciona esos archivos o informacion, los servidores de internet tiene la
    facilidad de usar sus servicios
     
}
